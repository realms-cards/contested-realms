:3010 {
    log
    encode zstd gzip
    header {
        -Server
    }
    # Load balance across the Socket.IO server instances
    reverse_proxy server1:3010 server2:3010 {
        lb_policy least_conn
    }
}
