:3010 {
    log
    encode zstd gzip
    header {
        -Server
    }
    # Load balance across the Socket.IO server instances
    reverse_proxy server1:3010 server2:3010 {
        # Stick each client IP to the same backend to avoid bouncing between instances
        lb_policy ip_hash
    }
}
