import { describe, it } from "vitest";

describe("Deck Submission Contract", () => {
  it.todo("submits a deck through the persistence manager");
  it.todo("persists drafted cards when submitting a deck");
  it.todo("validates tournament deck rules before submission");
  it.todo("persists submission state across route changes");
  it.todo("handles concurrent submissions safely");
  it.todo("tracks submission progress");
  it.todo("verifies submitted deck hashes");
  it.todo("handles submission timeouts");
});
