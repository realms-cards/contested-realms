import { describe, it } from "vitest";

describe("Draft Sync Pick Contract", () => {
  it.todo("processes a pick event through the sync manager");
  it.todo("synchronizes picks across clients");
  it.todo("resolves conflicting pick submissions");
  it.todo("broadcasts pick events to other participants");
  it.todo("enforces the pick timer");
  it.todo("rotates packs in sync after picks resolve");
});
