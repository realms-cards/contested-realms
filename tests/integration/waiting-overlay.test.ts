import { describe, it } from "vitest";

describe("Draft Waiting Overlay Event Contract", () => {
  it.todo("manages waiting overlay state");
  it.todo("shows the overlay immediately after deck submission");
  it.todo("updates player status indicators in real time");
  it.todo("calculates readiness progress correctly");
  it.todo("dismisses the overlay when all players are ready");
  it.todo("handles player disconnections while waiting");
  it.todo("renders contextual waiting messages");
  it.todo("handles timeout scenarios");
  it.todo("supports accessibility announcements and controls");
});
